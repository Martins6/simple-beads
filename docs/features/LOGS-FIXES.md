2026-08-03-21-53 | Fixed install.sh 404: remote (curl|bash) path now downloads the source tarball from main and builds it locally instead of attempting to download non-existent release binaries. Added `.github/workflows/release.yml` to cross-compile and attach binaries to GitHub Releases on `v*` tag push so the binary download path becomes viable once the first release is cut. Fixed `cd sbeads` → `cd simple-beads` typo in install.sh fallback messages and README.md clone instructions.
2026-08-22-14-30 | Addressed PR #3 automated review feedback: restored `rm -f "$BUILT_PATH"` cleanup in install.sh local mode (prevents stray ./sb in cwd); switched release workflow to `go-version-file: go.mod` so the Go version is single-source-of-truth and the first tagged release compiles; scoped `contents: write` to the `release` job only (build job now runs read-only); removed dead `if: github.event_name == "push"` on the Checksums step (workflow only triggers on push); fixed stale `cd sbeads` typo in skills/sbeats/SKILL.md; bumped README.md Go prerequisite 1.21 → 1.25.2 to match go.mod; added trailing newline to release.yml.
